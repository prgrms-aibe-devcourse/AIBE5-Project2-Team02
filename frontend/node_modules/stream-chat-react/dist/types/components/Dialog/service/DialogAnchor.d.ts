import type { ComponentProps, PropsWithChildren } from 'react';
import React from 'react';
import { type OffsetOpt } from '../hooks/usePopoverPosition';
import type { PopperLikePlacement } from '../hooks';
import type { Placement } from '@floating-ui/react';
export interface DialogAnchorOptions {
    open: boolean;
    placement: PopperLikePlacement;
    referenceElement: HTMLElement | null;
    allowFlip?: boolean;
    updateKey?: unknown;
    updatePositionOnContentResize?: boolean;
    offset?: OffsetOpt;
}
export declare function useDialogAnchor<T extends HTMLElement>({ allowFlip, offset, open, placement, referenceElement, updateKey, updatePositionOnContentResize, }: DialogAnchorOptions): {
    placement: Placement;
    setPopperElement: React.Dispatch<React.SetStateAction<T | null>>;
    styles: React.CSSProperties;
};
export type DialogAnchorProps = PropsWithChildren<Partial<DialogAnchorOptions>> & {
    /**
     * Optional per-dialog override for outside-click dismissal.
     * If undefined, manager-level `DialogManager.closeOnClickOutside` is used.
     */
    closeOnClickOutside?: boolean;
    id: string;
    /**
     * Delay (ms) before unmounting after dialog closes.
     * Use this to keep the dialog in DOM long enough for CSS exit animations.
     * `0` means immediate unmount (no exit-animation window).
     */
    closeTransitionMs?: number;
    dialogManagerId?: string;
    focus?: boolean;
    trapFocus?: boolean;
} & ComponentProps<'div'>;
export declare const DialogAnchor: ({ allowFlip, children, className, closeOnClickOutside, closeTransitionMs, dialogManagerId, focus, id, offset, placement, referenceElement, tabIndex, trapFocus, updateKey, updatePositionOnContentResize, ...restDivProps }: DialogAnchorProps) => import("react/jsx-runtime").JSX.Element | null;
//# sourceMappingURL=DialogAnchor.d.ts.map