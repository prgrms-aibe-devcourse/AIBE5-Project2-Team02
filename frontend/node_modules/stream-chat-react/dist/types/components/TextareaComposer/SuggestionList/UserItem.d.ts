import type { ComponentProps } from 'react';
export type UserItemProps = {
    /** The user */
    entity: {
        /** The parts of the Name property of the entity (or id if no name) that can be matched to the user input value.
         * Default is bold for matches, but can be overwritten in css.
         * */
        tokenizedDisplayName: {
            token: string;
            parts: string[];
        };
        /** Id of the user */
        id?: string;
        /** Image of the user */
        image?: string;
        /** Name of the user */
        name?: string;
    };
    focused?: boolean;
} & ComponentProps<'button'>;
/**
 * UI component for mentions rendered in suggestion list
 */
export declare const UserItem: ({ entity, focused: _, ...buttonProps }: UserItemProps) => import("react/jsx-runtime").JSX.Element | null;
//# sourceMappingURL=UserItem.d.ts.map